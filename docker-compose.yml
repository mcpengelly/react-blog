db:
  image: postgres
myapp:
  build: .
  ports:
   - "80:9000"
  links:
    - db
  environment:
    - PGHOST=db
    - PGDATABASE=postgres
    - PGUSER=postgres
    - BURNER_PASS=iAmABurner
    - DATABASE_URL=postgres://postgres:postgres@db:5432/postgres
  volumes:
    - /var/lib/postgres
